# E2E test environment — copy to .env.e2e and fill in values
# Never commit .env.e2e (it's in .gitignore)

# Base URL for the running app
E2E_URL=http://localhost:3000

# JARVIS has no authentication — no credentials needed
# Add any future API keys here if backend routes require them
